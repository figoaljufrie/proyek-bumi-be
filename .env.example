#run port on:
PORT=

#run node on dev / prod:
NODE_ENV=""

#SUPABASE connection:
#Connect via supabase pooling;
DATABASE_URL=""
# Direct connection to the database. Used for migrations
DIRECT_URL=""

# cloudinary
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

#Sending email (notification, verifications)
SENDGRID_API_KEY=
SENDGRID_SEND_EMAIL=

#For jwt secretkey (authenticate)
JWT_SECRET=
JWT_EXPIRES_IN=
